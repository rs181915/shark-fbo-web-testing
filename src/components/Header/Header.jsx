import React from 'react';

const Header = ({ title }) => (
  <div className="container mx-auto bg-primary py-2 rounded mt-16 mb-7"><p className="text-xl font-semibold text-[#fff] px-7">{title}</p></div>
);

export default Header;
